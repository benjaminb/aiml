[project]
name = "semantic-cache"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "redis>=7.1.0",
    "structured-output-llms",
]

[tool.uv.sources]
structured-output-llms = { workspace = true }
